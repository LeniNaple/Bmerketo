@*Account View Index*@


@{
    ViewData["Title"] = User.FindFirst("DisplayName")?.Value;
}


<div>
    @ViewData["Title"]
    <div>
        @{
            var useraa = User.Claims.FirstOrDefault(x => x.Type == "Role");
        }
        @useraa
    </div>
</div>